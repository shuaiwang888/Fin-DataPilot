# Dev / test / lint dependencies. NOT installed in the production HF Space image.
# Install locally with: pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.6.9
mypy==1.13.0
